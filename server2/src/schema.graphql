type User {
  googleID: String!
  credits: Number
}

type Survey {
  title: String!
  subject: String!
  body: String!
  recipients: [Recipient!]!
  yes: Number
  no: Number
  _user: User!
  dateSent: Date!
  lastResponded: Date
}

type Recipient {
  email: String!
  responded: Boolean
}